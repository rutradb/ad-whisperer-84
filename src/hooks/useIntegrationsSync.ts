/**
 * useIntegrationsSync
 *
 * Integration credentials are persisted via Zustand stores with localStorage.
 * This hook is a no-op placeholder kept for backward compatibility.
 */

export function useIntegrationsSync() {
  // Integrations are persisted in localStorage via Zustand stores.
  // No DB table needed.
}
